export default `-simple styled checkbox to match ns1 branding

- **label** *(string)* 
- **onChange** *(func)* called when checkbox toggles
- **checked** *(bool)* whether or not the thing is checked
- **name** *(string)* html name atribute
- **id** *(string)* id to use if you want to target the dom element directly
- **value** *(any)* value bolted on to change event
- **data-*** *(string)* any prop added that starts with data- will be treated like a data attribute
- **disabled** *(bool)* disable the checkbox
- **className** *(string)* if you want to add extra classes
- **help** *(string)* help / explainer text for what the checkbox is supposed to do 
`;
