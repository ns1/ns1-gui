export default `pagination helper

- **next** *(bool)* show next arrow after pagination items
- **prev** *(bool)* show prev arrow before pagination items
- **items** *(int)* number of pages available
- **maxButtons** *(int)* max number of links to display
- **activePage** *(int)* current index of active page
- **onSelect** *(func)* handler called when user selects a page
`;
