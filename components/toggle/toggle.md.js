export default `toggle switch component

- **label** *(string)* label for switch
- **checked** *(bool)* whether toggle is true/false
- **onChange** *(func)* callback for user interaction with switch
- **inline** *(bool)* display toggle and label on same level
- **labelFirst** *(bool)* show the label befor the togle in case of inline
`;
